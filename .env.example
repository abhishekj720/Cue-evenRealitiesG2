# Only used by summarize.py for the optional 6-word card blurb.
# If unset, enrollment and recognition work identically — blurbs just don't populate.
ANTHROPIC_API_KEY=sk-ant-...

# WebSocket bridge to the React plugin running in the Even app.
CUE_BRIDGE_HOST=127.0.0.1
CUE_BRIDGE_PORT=8765
